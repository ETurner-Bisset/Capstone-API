<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/weather-icons/2.0.12/css/weather-icons.min.css">
    <title>Your Weather</title>
</head>
<body>
    <div class="container px-4 py-5" id="custom-cards">
        <h2 class="pb-2 ">Your Current Weather</h2>
        <form class="search-bar" action="/submit" method="POST">
            <label for="location">Location: </label>
            <input type="text" placeholder="Enter location" name="location" id="location">
            <input type="submit" value="Search" class="btn btn-primary">
        </form>
    
        <div class="card text-bg-success mb-3" id="card" >
            <% if (locals.result) { %>
                <div class="card-header">Location: <%= result.location.name %><div class="date">Date: <%= result.location.localtime %></div></div>
            
            <div class="card-body">
                
                <p><%=result.current.condition.icon %></p>
              <p class="card-text"><%= result.current.condition.text %></p>
              <div class="temp">
                <h5>Temp:  <%= result.current.temp_c %> °C<h5>Feels like: <%= result.current.feelslike_c %>°C</h5></h5>
              </div>
              <div class="wind-uv">
                <p>Wind Speed: <%= result.current.wind_kph %> Kph </p>
                <p>Wind Direction: <%= result.current.wind_dir %></p>
                <p>UV Index: <%= result.current.uv %></p>
            </div>
           <% } else { %>
            <div class="card-body">
                <p>Please enter your location in the box above</p>
            </div>
           <% } %>
            
              
              
            </div>
          </div>
      </div>
</body>
</html>